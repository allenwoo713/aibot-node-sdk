---
phase: 02-http-fallback-transport
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/transport/http-transport.test.ts
  - src/transport/http-callback.test.ts
  - src/transport/fallback-transport.test.ts
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - Unit tests verify TokenCache refresh lock prevents concurrent token fetches
    - Unit tests verify HTTP message sending and single retry on 42001
    - Unit tests verify callback signature verification, decryption, deduplication, and stale timestamp rejection
    - Unit tests verify FallbackTransport switches outbound routing and deduplicates cross-transport messages
  artifacts:
    - path: src/transport/http-transport.test.ts
      provides: HttpTransport and TokenCache tests
    - path: src/transport/http-callback.test.ts
      provides: callback handler tests
    - path: src/transport/fallback-transport.test.ts
      provides: fallback routing and dedup tests
  key_links:
    - from: src/transport/http-transport.test.ts
      to: src/transport/http-transport.ts
      via: direct import
      pattern: "import.*HttpTransport"
    - from: src/transport/fallback-transport.test.ts
      to: src/transport/fallback-transport.ts
      via: direct import
      pattern: "import.*FallbackTransport"
---

<objective>
Add comprehensive unit and integration tests for the HTTP fallback transport, token cache, callback handler, and fallback routing logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-http-fallback-transport/02-CONTEXT.md
@.planning/phases/02-http-fallback-transport/02-RESEARCH.md
@.planning/phases/02-http-fallback-transport/02-PATTERNS.md
@.planning/phases/02-http-fallback-transport/02-02-SUMMARY.md
@.planning/phases/02-http-fallback-transport/02-03-SUMMARY.md

<interfaces>
From src/transport/http-transport.ts:
```typescript
export class HttpTransport extends EventEmitter<TransportEventMap> implements Transport {
  constructor(options: HttpTransportOptions);
  connect(): void;
  stop(): void;
  sendText(replyTo: WsFrame, text: string): Promise<void>;
  sendStream(replyTo: WsFrame, streamId: string, text: string, finish: boolean): Promise<void>;
  isConnected(): boolean;
}
```

From src/transport/http-callback.ts:
```typescript
export async function handleCallback(
  payload: CallbackPayload,
  crypto: WecomCrypto,
  emitter: Transport,
  logger?: Logger,
): Promise<CallbackResponse>
```

From src/transport/fallback-transport.ts:
```typescript
export class FallbackTransport extends EventEmitter<TransportEventMap> implements Transport {
  constructor(primary: WsTransport, fallback: HttpTransport, logger?: Logger);
}
```

From src/api.ts:
```typescript
export class WeComApiClient {
  async getAccessToken(corpid: string, corpsecret: string): Promise<{ access_token: string; expires_in: number }>;
  async sendTextMessage(token: string, agentid: string, touser: string, chatid: string | undefined, content: string): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HttpTransport and TokenCache unit tests</name>
  <files>src/transport/http-transport.test.ts</files>
  <read_first>src/transport/http-transport.ts, src/api.ts, src/types/transport.ts</read_first>
  <action>
    Create `src/transport/http-transport.test.ts` using vitest. Mock `WeComApiClient` methods with `vi.fn()`.

    Test cases to include:
    1. `TokenCache prevents concurrent token fetches`:
       - Create a mock `WeComApiClient` where `getAccessToken` returns a Promise that resolves after 50ms.
       - Create a `TokenCache` (you may need to export it from `http-transport.ts` or test via `HttpTransport` internals).
       - Fire two concurrent `getToken()` calls.
       - Assert `getAccessToken` was called exactly once.
       - Assert both calls resolve to the same token.

    2. `HttpTransport.sendText uses cached token`:
       - Mock `getAccessToken` to return `{ access_token: 'tok-1', expires_in: 7200 }`.
       - Mock `sendTextMessage` to resolve.
       - Call `httpTransport.sendText(mockFrame, 'hello')` twice.
       - Assert `getAccessToken` was called exactly once and `sendTextMessage` was called twice with token 'tok-1'.

    3. `HttpTransport.sendText retries once on 42001`:
       - Mock `getAccessToken` to first return `{ access_token: 'expired', expires_in: 7200 }`, then on second call return `{ access_token: 'fresh', expires_in: 7200 }`.
       - Mock `sendTextMessage` to reject with `new Error('send failed: token expired (42001)')` on first call, then resolve on second.
       - Call `httpTransport.sendText(mockFrame, 'hello')`.
       - Assert the call resolves (does not throw).
       - Assert `getAccessToken` was called twice.
       - Assert `sendTextMessage` was called twice: first with 'expired', second with 'fresh'.

    4. `HttpTransport.sendStream buffers and sends on finish`:
       - Mock `sendTextMessage` to resolve.
       - Call `sendStream(frame, 'sid-1', 'part1', false)` then `sendStream(frame, 'sid-1', 'part2', true)`.
       - Assert `sendTextMessage` was called exactly once with the concatenated text 'part1part2'.

    Use the following mock frame factory:
    ```typescript
    function createMockFrame(userid = 'user-1', chatid?: string): WsFrame {
      return {
        headers: { req_id: 'req-1' },
        body: { from: { userid }, chatid, msgid: 'm1' },
      } as WsFrame;
    }
    ```

    Note: If `TokenCache` is not exported, add `export { TokenCache }` at the bottom of `src/transport/http-transport.ts` so tests can import it directly.
  </action>
  <verify>
    <automated>npx vitest run src/transport/http-transport.test.ts</automated>
  </verify>
  <done>HttpTransport tests pass covering token cache serialization, 42001 retry, and stream buffering</done>
  <acceptance_criteria>
    - `src/transport/http-transport.test.ts` exists and `npx vitest run src/transport/http-transport.test.ts` exits 0
    - The test file contains a test case with the string `concurrent token fetches` or `TokenCache`
    - The test file contains `42001` in at least one test case
    - The test file contains `sendStream` in at least one test case
    - `src/transport/http-transport.ts` exports `TokenCache` (if it was not already exported)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add HTTP callback handler unit tests</name>
  <files>src/transport/http-callback.test.ts</files>
  <read_first>src/transport/http-callback.ts, src/wecom-crypto/index.ts, src/types/transport.ts</read_first>
  <action>
    Create `src/transport/http-callback.test.ts` using vitest.

    Test cases to include:
    1. `returns 403 when signature is invalid`:
       - Create a `WecomCrypto` instance with token='t', encodingAESKey='a valid 43-char base64 string padded to 44=' (use a real 43-char string plus '='), receiveId='r'.
       - Mock `crypto.verifySignature` to return `false` (or use a real crypto instance and pass a wrong signature).
       - Call `handleCallback({ signature: 'bad', timestamp: '1234567890', nonce: 'nonce', body: '{"Encrypt":"xxx"}' }, crypto, emitter)`.
       - Assert response is `{ status: 403, body: 'Forbidden' }`.

    2. `returns 403 when timestamp is stale`:
       - Use a real `WecomCrypto` instance.
       - Compute a signature for a payload with timestamp from 10 minutes ago (`Math.floor(Date.now()/1000) - 400`).
       - Call `handleCallback` with that timestamp.
       - Assert response status is 403.

    3. `decrypts payload and emits normalized frame on success`:
       - Use a real `WecomCrypto` instance.
       - Encrypt a JSON payload: `{"msgid":"m123","msgtype":"text","text":{"content":"hi"}}`.
       - Compute the correct signature for the encrypted payload.
       - Call `handleCallback` with the signature, current timestamp, nonce, and body containing the encrypt string in JSON envelope.
       - Assert response is `{ status: 200, body: 'success' }`.
       - Assert the emitter received `message.text` event with a `WsFrame` whose `body.msgid` is 'm123' and `cmd` is 'aibot_msg_callback'.

    4. `drops duplicate msgid and returns 200`:
       - Use the same setup as test 3.
       - Call `handleCallback` twice with the same payload.
       - Assert both return `{ status: 200, body: 'success' }`.
       - Assert the emitter's `message.text` handler was called exactly once.

    5. `handles XML envelope body`:
       - Use a real `WecomCrypto` instance.
       - Encrypt a JSON payload and compute signature.
       - Wrap the encrypt string in XML: `<xml><Encrypt>ENCRYPT</Encrypt></xml>`.
       - Call `handleCallback` with this XML body.
       - Assert response status is 200 and the emitter received the event.

    For the emitter mock, use an `EventEmitter<TransportEventMap>` instance from `eventemitter3` and spy on `.emit` with `vi.spyOn(emitter, 'emit')`.
  </action>
  <verify>
    <automated>npx vitest run src/transport/http-callback.test.ts</automated>
  </verify>
  <done>Callback handler tests pass covering signature verification, timestamp freshness, decryption, deduplication, and XML/JSON envelopes</done>
  <acceptance_criteria>
    - `src/transport/http-callback.test.ts` exists and `npx vitest run src/transport/http-callback.test.ts` exits 0
    - The test file contains `handleCallback` in the import or test descriptions
    - The test file contains `403` in at least one test case
    - The test file contains `msgid` in at least one test case
    - The test file contains `<Encrypt>` or `XML` in at least one test case
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add FallbackTransport unit tests</name>
  <files>src/transport/fallback-transport.test.ts</files>
  <read_first>src/transport/fallback-transport.ts, src/transport/ws-transport.ts, src/transport/http-transport.ts, src/types/transport.ts</read_first>
  <action>
    Create `src/transport/fallback-transport.test.ts` using vitest.

    Test cases to include:
    1. `routes sendText to primary when primary is connected`:
       - Create mock primary and fallback transports as `EventEmitter<TransportEventMap>` subclasses with `sendText = vi.fn().mockResolvedValue(undefined)`, `sendStream = vi.fn().mockResolvedValue(undefined)`, `isConnected = vi.fn()`, `connect = vi.fn()`, `stop = vi.fn()`.
       - Set `primary.isConnected.mockReturnValue(true)` and `fallback.isConnected.mockReturnValue(true)`.
       - Instantiate `FallbackTransport(primary, fallback)`.
       - Emit `connected` on primary.
       - Call `transport.sendText(mockFrame, 'hello')`.
       - Assert `primary.sendText` was called once and `fallback.sendText` was not called.

    2. `routes sendText to fallback when primary disconnects`:
       - Same setup as test 1, but after emitting `connected`, emit `disconnected` on primary.
       - Call `transport.sendText(mockFrame, 'hello')`.
       - Assert `fallback.sendText` was called once and `primary.sendText` was not called again.

    3. `deduplicates messages across primary and fallback`:
       - Instantiate `FallbackTransport(primary, fallback)`.
       - Emit `message.text` on primary with a frame whose `body.msgid` is 'm-dup'.
       - Emit `message.text` on fallback with the same `msgid`.
       - Assert the FallbackTransport emitted `message.text` exactly once (spy on `transport.emit`).

    4. `forwards non-message events from both transports`:
       - Emit `event.enter_chat` on primary and `event.template_card_event` on fallback.
       - Assert FallbackTransport emitted both events exactly once each.

    5. `routes sendStream to primary when connected`:
       - Emit `connected` on primary.
       - Call `transport.sendStream(mockFrame, 'sid-1', 'chunk', true)`.
       - Assert `primary.sendStream` was called with `(mockFrame, 'sid-1', 'chunk', true)`.

    Use the mock frame:
    ```typescript
    function createMockFrame(msgid = 'm1'): WsFrame {
      return {
        headers: { req_id: 'req-1' },
        body: { msgid, from: { userid: 'u1' } },
      } as WsFrame;
    }
    ```
  </action>
  <verify>
    <automated>npx vitest run src/transport/fallback-transport.test.ts</automated>
  </verify>
  <done>FallbackTransport tests pass covering routing logic and cross-transport deduplication</done>
  <acceptance_criteria>
    - `src/transport/fallback-transport.test.ts` exists and `npx vitest run src/transport/fallback-transport.test.ts` exits 0
    - The test file contains `FallbackTransport` in the import or test descriptions
    - The test file contains `primary` in at least one test case
    - The test file contains `fallback` in at least one test case
    - The test file contains `deduplicate` or `duplicate` in at least one test case
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks -> real crypto | Tests use real WecomCrypto for callback tests to verify correctness; mocks used for network boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | Test fixtures with fake AES keys | accept | Keys are randomly generated or hardcoded test-only values; no production secrets |
| T-02-11 | Denial of Service | TokenCache concurrent fetch test | mitigate | Test explicitly asserts single fetch call under concurrency to prevent regression |
</threat_model>

<verification>
- All transport tests pass: `npx vitest run src/transport/`
- Bot tests still pass: `npx vitest run src/bot/index.test.ts`
- TypeScript compilation passes: `npx tsc --noEmit`
</verification>

<success_criteria>
- HttpTransport tests verify token cache refresh lock and 42001 retry
- Callback handler tests verify signature verification, decryption, deduplication, and stale timestamps
- FallbackTransport tests verify routing and cross-transport deduplication
- All new tests pass without breaking existing test suites
</success_criteria>

<output>
After completion, create `.planning/phases/02-http-fallback-transport/02-04-SUMMARY.md`
</output>
