---
phase: 06-integration-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: true
requirements:
  - INTEG-03
  - INTEG-04
must_haves:
  truths:
    - "Docker image builds successfully end-to-end without errors"
    - "All 98 existing tests pass without regression"
  artifacts:
    - path: "Docker image"
      provides: "Production-ready container with SQLite support"
      verified_by: "docker build exits 0"
    - path: "Test suite"
      provides: "Regression-free verification"
      verified_by: "pnpm test exits 0 with 98 passing"
  key_links:
    - from: "Docker build"
      to: "Dockerfile"
      via: "docker build -t aibot-node-sdk ."
    - from: "Test suite"
      to: "src/index.ts exports"
      via: "TypeScript compilation and runtime imports"
---

<objective>
Verify the Docker image builds end-to-end after the Dockerfile fix, and confirm the full test suite passes without regression.

Purpose: Validate that the integration changes from 06-01 work correctly in practice and that no existing functionality is broken.
Output: Docker build success confirmation, test suite pass confirmation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-integration-deployment/06-01-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Prior Plan Output (06-01)
- Dockerfile production stage copies `node_modules` from builder instead of `pnpm install --prod`
- `src/index.ts` exports `ConversationStore`, `PersistenceBackend`, `JsonFileBackend`, `SqliteBackend`, `HistoryMessage`, `ConversationRecord`

## Current State
- Test command: `pnpm test` runs `vitest run`
- Phase 5 ended with 98/98 tests passing
- Docker build currently fails in production stage due to better-sqlite3 native compilation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Docker image builds end-to-end</name>
  <files></files>
  <read_first>
    - Dockerfile (verify 06-01 changes are in place)
  </read_first>
  <action>
    Run the Docker build command to verify the image builds successfully:

    ```bash
    docker build -t aibot-node-sdk:test .
    ```

    Expected behavior:
    - Builder stage completes successfully (installs dependencies, builds project)
    - Production stage copies `dist/` and `node_modules/` from builder
    - No compilation errors in production stage
    - Build exits with code 0

    If the build fails, diagnose the error:
    - If "better-sqlite3" compilation error: 06-01 Dockerfile fix was not applied correctly
    - If "module not found" error: check that `node_modules` copy includes the compiled native addon
    - If other error: report the specific error message
  </action>
  <verify>
    <automated>docker build -t aibot-node-sdk:test .</automated>
  </verify>
  <done>Docker image builds successfully with exit code 0, no better-sqlite3 compilation errors in production stage</done>
  <acceptance_criteria>
    - `docker build -t aibot-node-sdk:test .` exits with code 0
    - Build output does NOT contain "g++" or "make" errors in the production stage
    - Build output does NOT contain "better-sqlite3" compilation errors
    - Final image size is reasonable (builder stage + production stage, production should be smaller)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify no regression</name>
  <files></files>
  <read_first>
    - package.json (confirm test script is `vitest run`)
  </read_first>
  <action>
    Run the full test suite to confirm all existing tests still pass:

    ```bash
    pnpm test
    ```

    Expected behavior:
    - All 98 tests from Phase 5 continue to pass
    - No new test failures introduced by the 06-01 export changes
    - Exit code is 0

    If tests fail:
    - Check if the failure is related to the new exports in `src/index.ts` (e.g., circular dependency, missing type export)
    - Check if any test imports from `src/index.ts` and now resolves differently
    - Report the specific failing test names and error messages
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>All 98 tests pass, exit code 0, no regressions from Phase 5</done>
  <acceptance_criteria>
    - `pnpm test` exits with code 0
    - Test output shows at least 98 tests passing
    - No test failures or errors in the output
    - No TypeScript compilation errors during test run
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/build environment -> runtime | Build artifacts must be verified before deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | Docker build failure in CI | mitigate | Explicit docker build verification catches Dockerfile issues before they reach CI/production |
| T-06-04 | Information Disclosure | Test failures expose internal state | accept | Test output is local/dev only; no PII in test suite |
</threat_model>

<verification>
1. `docker build -t aibot-node-sdk:test .` — exits 0
2. `pnpm test` — exits 0 with 98+ passing tests
</verification>

<success_criteria>
- Docker image builds successfully end-to-end after Dockerfile fix
- Full test suite passes (98/98 or more) with no regressions
- Both verification commands exit with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-deployment/06-02-SUMMARY.md`
</output>
