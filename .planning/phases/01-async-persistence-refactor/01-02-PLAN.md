---
phase: 01-async-persistence-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/bot/index.ts
  - src/memory.test.ts
  - src/config/index.ts
autonomous: true
requirements:
  - COMPAT-02
  - TEST-01
must_haves:
  truths:
    - "BotOrchestrator awaits ConversationStore mutating methods without breaking startup flow"
    - "Unit tests verify concurrent append serialization, lazy initialization, corrupt-file recovery, and logger warnings"
    - "Config ensures persistence directory exists (can stay sync in loadConfig)"
  artifacts:
    - path: "src/bot/index.ts"
      provides: "BotOrchestrator with awaited store calls"
    - path: "src/memory.test.ts"
      provides: "Unit tests for async persistence behaviors"
  key_links:
    - from: "BotOrchestrator.handleTextMessage"
      to: "ConversationStore.append"
      via: "await this.store.append(...)"
    - from: "memory.test.ts"
      to: "ConversationStore"
      via: "vitest assertions on async behavior"
---

<objective>
Update `BotOrchestrator` to `await` the now-async `ConversationStore` mutating methods, and expand `src/memory.test.ts` with unit tests for concurrent write serialization, lazy initialization, corrupt-file recovery, and logger warnings.

Purpose: Ensure internal consumers respect the new async persistence contract and verify correctness with automated tests.
Output: Updated `src/bot/index.ts`, expanded `src/memory.test.ts`, and optionally `src/config/index.ts` if logger wiring is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-async-persistence-refactor/01-CONTEXT.md
@src/bot/index.ts
@src/memory.test.ts
@src/config/index.ts
@src/types/common.ts

## Key Interfaces

From `src/types/common.ts`:
```typescript
export interface Logger {
  debug(message: string, ...args: any[]): void;
  info(message: string, ...args: any[]): void;
  warn(message: string, ...args: any[]): void;
  error(message: string, ...args: any[]): void;
}
```

From `src/memory.ts` (after Plan 01):
- `append(conversationId, message): Promise<void>`
- `clear(conversationId): Promise<void>`
- `clearAll(): Promise<void>`
- `save(): Promise<void>`
- Constructor accepts `{ logger?: Logger }` in addition to the BotConfig pick.

## BotOrchestrator Call Sites

In `src/bot/index.ts`:
- Line 92: `this.store.append(conversationId, { role: 'assistant', content: result.content });` — must become `await this.store.append(...)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire logger into BotOrchestrator and update async store calls</name>
  <files>src/bot/index.ts, src/config/index.ts</files>
  <action>
    1. In `src/bot/index.ts`:
       - Import `DefaultLogger` from `../logger` if you want a fallback, or just pass `undefined`. Recommended: import `DefaultLogger` and instantiate it in the constructor: `this.logger = config.logger ?? new DefaultLogger('BotOrchestrator');`
       - Update `BotOrchestrator` to hold `private logger: Logger;`.
       - When constructing `ConversationStore`, pass the logger:
         ```typescript
         this.store = new ConversationStore({ ...config, logger: this.logger });
         ```
       - Find the call `this.store.append(conversationId, { role: 'assistant', content: result.content });` inside `handleTextMessage` and add `await` before it.
       - Check if there are any other `this.store.append`, `this.store.clear`, or `this.store.clearAll` calls in the file. If so, add `await` to each.
    2. In `src/config/index.ts`:
       - Add `logger?: Logger` to the `BotConfig` interface. This makes it natural for external consumers to inject a logger if they want persistence warnings.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `src/bot/index.ts` compiles with zero errors.
    - `BotOrchestrator` passes a logger into `ConversationStore`.
    - All `ConversationStore` mutating method calls in `BotOrchestrator` are awaited.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for async persistence behaviors</name>
  <files>src/memory.test.ts</files>
  <behavior>
    - Test 1 (lazy init): Create a store with a pre-existing persistence file. Call `store.get('c1')` immediately (sync). Then create a second store and `await store.append('c2', ...)` — the second store should load the pre-existing file. To verify lazy init precisely: spy on `fs.readFile` (or mock `fs/promises`) and assert that `new ConversationStore(...)` does NOT trigger `readFile`, but the first `await store.append(...)` does.
    - Test 2 (concurrent write serialization): Create a store and fire 5 concurrent `await store.append('c1', ...)` calls with different contents. After all settle, read the persistence file and assert it contains valid JSON with all 5 messages in order. Additionally, mock `fs.writeFile` to track calls and assert that writes were serialized (no overlapping promises).
    - Test 3 (corrupt file recovery): Write invalid JSON (e.g., `{"broken"`) to the persistence path. Create a store, `await store.append('c1', { role: 'user', content: 'hello' })`. Assert that `store.get('c1')` returns the appended message (corrupt state was discarded, new state written). Assert that the injected logger received a `warn` call containing "load conversation state".
    - Test 4 (logger warning on save error): Mock `fs.writeFile` to reject with an error. Call `await store.append('c1', ...)`. Assert the method does not throw and the logger received a `warn` call containing "save conversation state".
  </behavior>
  <action>
    Expand `src/memory.test.ts` with the following tests using vitest and `vi` mocks.

    1. Add imports at the top:
       ```typescript
       import fs from 'fs/promises';
       import { vi } from 'vitest';
       ```
    2. Add a helper to create a mock logger:
       ```typescript
       function createMockLogger() {
         return {
           debug: vi.fn(),
           info: vi.fn(),
           warn: vi.fn(),
           error: vi.fn(),
         };
       }
       ```
    3. Update `createStore` to accept an optional logger and pass it through:
       ```typescript
       function createStore(ttlMs = 60000, maxConversations = 1000, maxHistory = 20, logger?: any) {
         return new ConversationStore({
           conversationTtlMs: ttlMs,
           maxConversations,
           maxHistoryMessages: maxHistory,
           persistencePath: TEST_PERSISTENCE_PATH,
           logger,
         });
       }
       ```
    4. Update all existing test instantiations to `await` mutating calls where needed (e.g., `store.append` becomes `await store.append`). Since existing tests call `append`, `clear`, etc., add `async` to the test callback and `await` those calls.
    5. Add new test block `describe('async persistence', () => { ... })` with:
       - `it('lazy initializes on first mutation', async () => { ... })`
       - `it('serializes concurrent writes', async () => { ... })`
       - `it('recovers from corrupt persistence file and logs warning', async () => { ... })`
       - `it('logs warning on save failure without throwing', async () => { ... })`
    6. For the concurrent-write test, use `vi.spyOn(fs, 'writeFile')` to count overlapping calls. Because writes are queued, at no point should two `writeFile` promises be in-flight simultaneously. One way to verify: make `writeFile` return a Promise that resolves after a short delay, and assert that the next call only happens after the previous one resolves.
    7. Ensure `beforeEach` and `afterEach` still clean up the test persistence file using sync `fs` from `node:fs` (tests are allowed to use sync fs for setup/teardown).
  </action>
  <verify>
    <automated>npm test -- --filter=memory</automated>
  </verify>
  <done>
    - `src/memory.test.ts` contains at least 4 new tests covering lazy init, concurrent write serialization, corrupt-file recovery, and save-error logging.
    - All memory tests pass.
    - No test uses production persistence paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test filesystem -> test assertions | Tests write arbitrary JSON to disk and assert recovery behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | test logger mock captures warn messages | accept | Mock logger is internal to test suite; no data leaks outside test process. |
| T-01-07 | Denial of Service | concurrent write test could leave temp files | accept | `afterEach` cleans up `TEST_PERSISTENCE_PATH`; tests run in isolated vitest workers. |
</threat_model>

<verification>
- Run `npm test` and confirm 100% of memory tests pass.
- Run `npx tsc --noEmit` and confirm zero TypeScript errors across the project.
- Run `grep -n "store\.append\|store\.clear\|store\.clearAll" src/bot/index.ts` and verify each call is prefixed with `await`.
</verification>

<success_criteria>
- `BotOrchestrator` awaits all `ConversationStore` mutating methods.
- `BotConfig` includes an optional `logger` field.
- Unit tests prove lazy initialization, write-queue serialization, corrupt-state recovery, and non-throwing save errors with logger warnings.
- Full test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-async-persistence-refactor/01-02-SUMMARY.md`
</output>
