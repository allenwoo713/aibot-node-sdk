---
context: phase
phase: 06-integration-deployment
task: 0
total_tasks: 3
status: not_started
last_updated: "2026-04-20T00:00:00.000Z"
---

# Continue Here — Phase 6: Integration & Deployment

## Current State

Phase 5 is **COMPLETE** (3 plans executed, 98/98 tests passing, all committed). We are at the boundary between Phase 5 completion and Phase 6 start.

Phase 6 has been **re-scoped** based on Phase 5 execution (much of its original scope was absorbed into Phase 5). Remaining work:

1. **Fix Dockerfile** — Production stage cannot install better-sqlite3 without build tools. Need to copy `node_modules` from builder stage instead of running `pnpm install --prod`.
2. **Export persistence layer** from `src/index.ts` — `PersistenceBackend`, `JsonFileBackend`, `SqliteBackend`, `ConversationStore`, `HistoryMessage`, `ConversationRecord`.
3. **Verify Docker build** — Run `docker build` to confirm the image builds end-to-end.

## Completed Work (Phase 5)

- 05-01: PersistenceBackend interface + JsonFileBackend + SqliteBackend (WAL, migration)
- 05-02: Refactored ConversationStore with pluggable backend, config/env wiring
- 05-03: Async BotOrchestrator.stop() + entry.ts graceful shutdown + 19 new tests

## Decisions Made

- PersistenceBackend isolates storage from ConversationStore
- Migration renames (not deletes) original JSON file
- SQLite path derived from persistencePath via `.replace(/\.json$/, '.db')`
- Dockerfile builder stage has python3 make g++, but production stage is broken (needs fix)

## Blockers

None.

## Next Action

Run `/gsd-plan-phase 6` to create Phase 6 plans, then execute.

Alternative: Skip planning and execute inline since scope is small and well-defined (3 tasks, no dependencies).

## Required Reading

1. `src/index.ts` — current public API exports
2. `Dockerfile` — current multi-stage build (has the production-stage bug)
3. `.planning/ROADMAP.md` — re-scoped Phase 6 success criteria
